<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hwhong.balance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.EmailActivity" />
        <activity android:name=".Views.ProfileActivity" />
        <activity android:name=".Views.NameActivity" />
        <activity android:name=".Views.PhoneActivity" />
        <activity android:name=".Views.VerificationActivity" />
        <activity android:name=".Views.DobActivity" />
        <activity android:name=".Views.AddressActivity" />
        <activity android:name=".Views.CitizenshipActivity" />
        <activity android:name=".Views.SSNActivity" />
        <activity android:name=".Views.CompletionActivity"></activity>
    </application>

</manifest>